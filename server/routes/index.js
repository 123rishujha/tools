const compressorRouter = require("./compressorRouter");
const imageUploadRouter = require("./compressorRouter");

const routes = {
  compressorRouter,
  imageUploadRouter,
};

module.exports = routes;
